# Dependencies
node_modules/
*/node_modules/

# Environment files — NEVER commit these
.env
.env.*
*.env
editions/*/.env
haven/src/.env

# Seed data — business logic, keep private
data/seed/
scripts/seed*
data/field-config.json
data/framework-versions.json

# AI prompt guardrails — proprietary
config/ai-prompts/
src/prompts/

# Uploads and media
data/uploads/
data/ladn/
data/ladn-dev/
wren-data/

# Logs
*.log
logs/
build-logs/

# Database dumps
*.sql.gz
backups/
backups-overnight-*/

# Build artifacts
*.pid
.DS_Store

# Feedback data
wren-feedback/

# n8n workflow exports with credentials
n8n-workflows/*.json

# Legacy Haven stack (retired, pre-Wren) — root-level dir only.
# editions/haven/ (care-home edition, 2026-07-12) IS tracked.
/haven/

# LADN-specific config
editions/ladn/.env
editions/ladn-admin/.env
editions/parents/.env

# Secrets
secrets/

# === Hardening 2026-06-08 — never commit data/dumps/backups/exports ===
wren-backups/
*.sql.gz
*.bak
*.bak*
*.pre-*
dm-populate.sql
*.bak-trackers-*.sql
.vscode-server/
.vscode/
*.cache
.cache
.cache/
# scraped real data (PII) — never commit
eylog-export/
output/
/opt/eylog-scraper/output/

# === Clean-repo hardening 2026-06-08 (Task 2) ===
# Env files: exclude every .env variant, but ALWAYS keep *.env.example templates.
**/.env
**/.env.*
**/*.env
!**/*.env.example
!.env.example

# All upload / media / generated-asset dirs (PII: child photos, observations, audio)
data/uploads/
data/*/uploads/
data/**/uploads/
**/uploads/
**/photos/
**/audio/
**/media/
**/memory-box/

# Production / per-tenant data trees
data/ladn/
data/eyfs/
data/secondary/
data/primary/

# Data dumps & exports (KEEP real source migrations under migrations/, data/migrations/,
# src/db/migrations/ and docker/**/init|initdb schema files — those are source, not dumps)
*.dump
*.sql.bak
*.sqlite
*.sqlite-*
*.sqlite.gz
exports/
export/
scraped/

# Misc local/editor cruft
.DS_Store
*.tmp
*.swp
.idea/

# Nested/embedded scratch repo — not part of the Wren codebase
external-scan/
docker/postgres/data/

# Real personal data — backed up via DB dumps + drive-mirror, NEVER in git (GDPR)
data/brighthr-*
data/smoke-*.png
data/parents-resources/

# Data governance encrypted archives (may contain PII — never commit)
governance-archives/
editions/admin/public/documents/reports/
