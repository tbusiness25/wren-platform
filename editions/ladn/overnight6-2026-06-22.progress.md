# Overnight6 Build Progress — 2026-06-22

## Completed

- **A.1. Framework tracker UI** — Standalone `/framework-tracker.html` for EY cohort. Deployed to ladn/public and verified 200 response. Has zero data rows in `framework_tracker` table (table exists but data hasn't been populated yet).
- **A.2. Child-profile Tracker tab** — Added "Tracker" tab between Attendance and Details on `child-profile.html`. Added `loadFramework()` and `renderFrameworkGrid()` with RAG coloring. Deployed and verified.
- **A.3. Staff performance overview** — `GET /api/performance/overdue` endpoint returns per-staff overdue observations, settling-in overdue, and 6-month review due counts. Deployed to wren-ladn.
- **A.7. /overdue endpoint fix (pg rows destructure)** — Fixed 500 error where `obsOverdue` was assigned the full `{ rows: [...] }` result from `db.query()` instead of the `.rows` array. Changed all three query declarations from `const obsOverdue = await db.query(...)` to `const { rows: obsOverdue } = await db.query(...)`. Verified endpoint returns 5 overdue observations across 3 staff members.

## Pending / Deferred

- **A.4.** Population of `framework_tracker` table (needs data migration or manual entry)
- **A.6.** Staff performance supervision pack — partially complete
- **A.5.** Review cycle for framework progress tracking

## Phase B — Demo Seeding (2026-06-22)

### Status summary (row counts after seeding)

| Schema   | children | staff | parent_portal | observations | daily_diary | attendance | messages | next_steps | incidents | reports | framework_tracker | weekly_plans |
|----------|----------|-------|---------------|--------------|-------------|------------|----------|------------|-----------|---------|-------------------|--------------|
| **demo_eyfs**      | 30       | 17    | 30            | 191          | 1209        | 2580       | 40       | 181        | 64        | 2       | 90                | —            |
| **demo_primary**   | 90       | 5     | 10            | 270          | 644         | 7740       | 7        | 30         | 5         | 8       | 160               | —            |
| **demo_secondary** | 90       | 25    | 100           | 1085         | 573         | 7740       | 7        | 30         | 5         | 8       | 160               | 20           |

### Items completed

- **B.1. EYFS patch** — Added 12 staff (room_leaders, practitioners, apprentice, admin, cook → staff 5→17), 30 messages (10→40), 180 next_steps per framework area (1→181), 20 incidents (4→64) and verified all parent portal entries (30 children covered). SQL: `scripts/demo-seed-eyfs-patch.sql`. Verified in wren postgres.
- **B.2. Primary daily_diary** — Added 644 daily diary entries for 90 children across 10 weekdays (1→644). Fixed staff_id FK issues, timestamp interval syntax, constraint conflict (dd_child_date). SQL: `scripts/demo-seed-primary-daily-diary.sql`. Verified in wren postgres.
- **B.3. Secondary daily_diary + weekly_plans** — Added 573 daily diary rows (1→573) and 20 weekly_plans (0→20) with room_id FK mapping. SQL: `scripts/demo-seed-secondary-dd-wp.sql`. Verified in wren postgres.

### Items still pending

- **B.4. Primary: staff (25→70 total), parent_portal (90 entries), messages (+20), incidents (+20)** — Primary only has 5 staff and 10 parent_portal entries vs 90 children.
- **B.5. Secondary: messages (+20), incidents (+20), next_steps (+100)** — Secondary has good base but needs more messages/incidents/next_steps for realistic demo.
- **B.6. Portal rendering verification** — Ports 8192/8193/8194 returned 404/401. Demo frontend endpoints may not be exposed on those ports in this deployment. Data verified in wren postgres directly.
- **B.7. Ladn tables verified untouched** — ladn.* unchanged (270 tables, no modifications made).
